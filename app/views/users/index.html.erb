<p style="color: green"><%= notice %></p>

<h1>Users</h1>

<div id="users">
  <% @users.each do |user| %>
    <%= render user %>
    <p>
      Number of Posts: <%= user.posts.count %>
    </p>
    <p>
      <%= link_to "Show this user", user %>
    </p>
  <% end %>
</div>
